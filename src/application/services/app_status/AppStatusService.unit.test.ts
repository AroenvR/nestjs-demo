describe.skip("AppStatusService.Unit", () => {
	// TODO
	beforeAll(async () => {});

	beforeEach(async () => {});

	afterEach(async () => {});

	afterAll(async () => {});

	it("Yolo", () => {
		expect("WIP").toEqual(true);
	});
});
