describe("AppStatusController.Unit", () => {
    beforeAll(async () => {

    });

    beforeEach(async () => {

    });

    afterEach(async () => {

    });

    afterAll(async () => {

    });

    it("Can yolo", () => {
        expect("WIP").toEqual(true);
    });
});