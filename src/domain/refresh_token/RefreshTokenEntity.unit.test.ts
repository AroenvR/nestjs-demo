describe("RefreshTokenEntity.Unit", () => {
	beforeAll(async () => {});

	beforeEach(async () => {});

	afterEach(async () => {});

	afterAll(async () => {});

	it("Yolo", () => {
		expect("WIP").toEqual(true);
	});
});

/*
`-
Snippet:

"Yolo? No. Create a test that stops an empty suite from throwing": {
    "prefix": "yolo",
    "body": [
        "describe(\"$1\", () => {",
        "    beforeAll(async () => {",
        "",
        "    });",
        "",
        "    beforeEach(async () => {",
        "",
        "    });",
        "",
        "    afterEach(async () => {",
        "",
        "    });",
        "",
        "    afterAll(async () => {",
        "",
        "    });",
        "",
        "    it(\"Can yolo\", () => {",
        "        expect(\"WIP\").toEqual(true);",
        "    });",
        "});",
    ],
    "description": "Create a test that stops an empty suite from throwing an error",
}

*/
